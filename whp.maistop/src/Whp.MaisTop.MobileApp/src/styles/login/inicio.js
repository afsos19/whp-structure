import colors from 'res/colors';

const inicio = {
    heading: {
        fontFamily: 'Montserrat',
        fontWeight: 'Light',
        fontSize: 25,
        color: colors.textLight,
        textAlign: 'center',
    },
    input: {
        fontFamily: 'Montserrat',
        fontWeight: 'Bold',
        fontSize: 13,
        color: colors.text,
        textAlign: 'center',
    },
};

export default inicio;
